<UserControl x:Class="FigCrafterApp.Views.CanvasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FigCrafterApp.Views"
             xmlns:skia="clr-namespace:SkiaSharp.Views.WPF;assembly=SkiaSharp.Views.WPF"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Focusable="True"
             KeyDown="CanvasView_KeyDown">
    <Grid Background="#FF2D2D30">
        <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <Grid Width="{Binding WidthPx}" Height="{Binding HeightPx}" Background="White" Margin="40">
                <Grid.Effect>
                    <DropShadowEffect BlurRadius="15" Direction="270" Opacity="0.5" ShadowDepth="5"/>
                </Grid.Effect>
                
                <skia:SKElement x:Name="SkiaElement" 
                                PaintSurface="SkiaElement_PaintSurface"
                                MouseLeftButtonDown="SkiaElement_MouseLeftButtonDown"
                                MouseMove="SkiaElement_MouseMove"
                                MouseLeftButtonUp="SkiaElement_MouseLeftButtonUp"
                                DataContextChanged="SkiaElement_DataContextChanged"/>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
